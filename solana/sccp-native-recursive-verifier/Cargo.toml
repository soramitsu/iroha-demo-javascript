[package]
name = "sccp-native-recursive-verifier"
version = "0.1.0"
edition = "2021"
description = "Fail-closed Solana native recursive verifier CPI target for TAIRA SCCP evidence"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "4.0.0"
